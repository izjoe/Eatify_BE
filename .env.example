# Server Configuration
PORT=4000

# Database
MONGO_URI=mongodb://localhost:27017/eatify

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Frontend URL (deployed frontend)
# Set this to your production frontend URL. Default points to the deployed frontend used by the team.
FRONTEND_URL=https://eatify-fe.vercel.app

# ==========================================
# Email Configuration (SMTP)
# ==========================================
# To send emails, configure the following environment variables:

# SMTP Server Host (ví dụ: smtp.gmail.com, smtp.outlook.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Email account to send from
SMTP_USER=your_email@gmail.com

# Password or App Password
# Note: For Gmail you should create an App Password at:
# https://myaccount.google.com/apppasswords
SMTP_PASS=your_app_password_here

# Sender email address shown to recipients
SMTP_FROM="Eatify <noreply@eatify.com>"

# ==========================================
# Gmail SMTP setup guide:
# ==========================================
# 1. Enable "2-Step Verification" in your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Create an App Password for your application
# 4. Copy the generated password into SMTP_PASS (16 chars)
# 5. Use the following values:
#    SMTP_HOST=smtp.gmail.com
#    SMTP_PORT=587
#    SMTP_USER=your_gmail@gmail.com

# ==========================================
# Test with Ethereal (fake SMTP):
# ==========================================
# Visit: https://ethereal.email/create
# Copy the generated SMTP credentials into your .env:
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=(ethereal username)
# SMTP_PASS=(ethereal password)
