# Server Configuration
PORT=4000

# Database
MONGO_URI=mongodb://localhost:27017/eatify

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Frontend URL
FRONTEND_URL=http://localhost:3000

# ==========================================
# Email Configuration (SMTP)
# ==========================================
# Để gửi email, cần cấu hình các biến sau:

# SMTP Server Host (ví dụ: smtp.gmail.com, smtp.outlook.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 cho TLS, 465 cho SSL)
SMTP_PORT=587

# Email account để gửi
SMTP_USER=your_email@gmail.com

# Password hoặc App Password
# Lưu ý: Với Gmail, bạn cần tạo App Password tại:
# https://myaccount.google.com/apppasswords
SMTP_PASS=your_app_password_here

# Địa chỉ email hiển thị khi gửi
SMTP_FROM="Eatify <noreply@eatify.com>"

# ==========================================
# Hướng dẫn cấu hình Gmail SMTP:
# ==========================================
# 1. Bật "2-Step Verification" trong Google Account
# 2. Truy cập: https://myaccount.google.com/apppasswords
# 3. Tạo App Password mới cho ứng dụng
# 4. Copy password đó vào SMTP_PASS (16 ký tự, không có dấu cách)
# 5. SMTP_HOST=smtp.gmail.com
# 6. SMTP_PORT=587
# 7. SMTP_USER=your_gmail@gmail.com

# ==========================================
# Hướng dẫn test với Ethereal (fake SMTP):
# ==========================================
# Truy cập: https://ethereal.email/create
# Lấy thông tin SMTP và điền vào:
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=(username từ ethereal)
# SMTP_PASS=(password từ ethereal)
